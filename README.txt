# Navin Noronha — interactive portfolio

Open `index.html` in a browser.

The `assets/` folder contains visual assets extracted from the supplied Navin Noronha portfolio PDF and used by the prototype.

Before production:
- replace placeholder venture/podcast copy with approved copy;
- add final podcast, social, ticketing and project URLs;
- add any preferred corporate client names/logos;
- replace the small "ask for credits" links with final destinations;
- optimise the extracted PNGs to WebP/AVIF for deployment.

The page is intentionally built as a new visual system rather than a clone of the Abhi Tundel portfolio: stage/poster/editorial/radio motifs, scroll reveals, an intro screen, a tour timeline, record-style podcast section, venture board and corporate section.
